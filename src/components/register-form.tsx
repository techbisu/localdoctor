'use client'import { useState } from 'react'import { useForm } from 'react-hook-form'import { zodResolver } from '@hookform/resolvers/zod'import { z } from 'zod'import { Button } from '@/components/ui/button'import { Input } from '@/components/ui/input'import { Label } from '@/components/ui/label'import { Card, CardContent } from '@/components/ui/card'import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select'import { registerAction } from '@/server/actions/auth'import { toast } from 'sonner'import { useRouter } from 'next/navigation'import Link from 'next/link'const schema = z.object({  name: z.string().min(2, 'Enter your full name'),  email: z.string().email('Enter a valid email'),  password: z.string().min(6, 'Password must be at least 6 characters'),  role: z.enum(['DOCTOR', 'PHARMACY', 'LAB']),  entityName: z.string().min(2, 'Enter practice name'),  phone: z.string().min(10, 'Enter valid phone number'),  city: z.string().min(2, 'Enter city'),  address: z.string().min(5, 'Enter full address'),})type FormData = z.infer<typeof schema>export function RegisterForm() {  const [loading, setLoading] = useState(false)  const router = useRouter()  const { register, handleSubmit, setValue, watch, formState: { errors } } = useForm<FormData>({ resolver: zodResolver(schema) })  const onSubmit = async (data: FormData) => {    setLoading(true)    const formData = new FormData()    Object.entries(data).forEach(([key, value]) => formData.append(key, value))    const result = await registerAction(formData)    setLoading(false)    if (result.error) {      toast.error(result.error)    } else {      toast.success('Registration submitted! Your account is pending approval.')      router.push('/login')    }  }  return (    <Card><CardContent className='pt-6'>      <form onSubmit={handleSubmit(onSubmit)} className='space-y-4'>        <div><Label>I am a</Label><Select onValueChange={(v) => setValue('role', v as any)} defaultValue={watch('role')}><SelectTrigger><SelectValue placeholder='Select provider type' /></SelectTrigger><SelectContent><SelectItem value='DOCTOR'>Doctor</SelectItem><SelectItem value='PHARMACY'>Pharmacy / Medicine Shop</SelectItem><SelectItem value='LAB'>Medical Test Lab</SelectItem></SelectContent></Select>{errors.role && <p className='text-sm text-red-500 mt-1'>{errors.role.message}</p>}</div>        <div className='grid grid-cols-2 gap-4'><div><Label>Your Name</Label><Input {...register('name')} placeholder='Full name' />{errors.name && <p className='text-sm text-red-500 mt-1'>{errors.name.message}</p>}</div><div><Label>Practice/Shop Name</Label><Input {...register('entityName')} placeholder='Business name' />{errors.entityName && <p className='text-sm text-red-500 mt-1'>{errors.entityName.message}</p>}</div></div>        <div><Label>Email</Label><Input type='email' {...register('email')} placeholder='you@example.com' />{errors.email && <p className='text-sm text-red-500 mt-1'>{errors.email.message}</p>}</div>        <div><Label>Password</Label><Input type='password' {...register('password')} placeholder='******' />{errors.password && <p className='text-sm text-red-500 mt-1'>{errors.password.message}</p>}</div>        <div className='grid grid-cols-2 gap-4'><div><Label>Phone</Label><Input {...register('phone')} placeholder='Mobile number' />{errors.phone && <p className='text-sm text-red-500 mt-1'>{errors.phone.message}</p>}</div><div><Label>City</Label><Input {...register('city')} placeholder='City name' />{errors.city && <p className='text-sm text-red-500 mt-1'>{errors.city.message}</p>}</div></div>        <div><Label>Full Address</Label><Input {...register('address')} placeholder='Street address' />{errors.address && <p className='text-sm text-red-500 mt-1'>{errors.address.message}</p>}</div>        <Button type='submit' className='w-full bg-teal-700 hover:bg-teal-800' disabled={loading}>{loading ? 'Submitting...' : 'Submit Registration'}</Button>        <p className='text-center text-sm text-muted-foreground'>Already have an account? <Link href='/login' className='text-teal-700 hover:underline'>Sign In</Link></p>      </form>    </CardContent></Card>  )}